# Benchmark Publishing Implementation Summary

## 📋 实现内容

基于你的需求，我实现了一个**简单、专注的 Benchmark 发布方案**。

### 核心需求理解

```
场景: 运行 Benchmark 测试
    │
    ├─ 修改了 SDK 代码 (修复 bug)
    ├─ 需要发布到 npm
    ├─ 在 Daytona 上安装最新版本
    └─ 运行 benchmark 测试

关键点:
    ✓ CLI 依赖 SDK
    ✓ 需要保证 CLI 使用最新的 SDK
    ✓ 快速迭代，可能一天发布多次
```

## 🎯 解决方案

### 一键发布脚本

创建了 `scripts/publish-benchmark.ts`，一个命令完成所有发布步骤：

```bash
bun run publish:benchmark
```

### 自动化流程

```
1. 生成 canary 版本号 (带时间戳)
   └─ 0.1.0-canary.202602271430

2. 更新 SDK 版本

3. 运行 SDK 测试

4. 构建 SDK

5. 发布 SDK 到 npm (--tag canary)

6. 等待 npm 索引 (轮询检查)

7. 更新 CLI 版本和依赖

8. 发布 CLI 到 npm (--tag canary)

9. 输出安装命令
```

## 📁 创建的文件

### 1. 核心脚本
```
scripts/publish-benchmark.ts
```
- 自动化发布脚本
- 包含错误处理和进度提示
- 支持未提交更改警告

### 2. 文档
```
docs/
├── BENCHMARK-PUBLISHING.md    # 完整使用指南
├── PUBLISH-FLOW.md            # 流程详解和场景分析
├── QUICK-PUBLISH.md           # 快速参考卡片
└── publishing/
    └── README.md              # 文档导航
```

### 3. 配置更新
```
package.json
```
- 添加 `publish:benchmark` 命令

## 🎨 设计特点

### 1. Canary 版本策略

```
格式: 0.1.0-canary.202602271430
      └─────┘       └──────────┘
      基础版本        时间戳

优点:
✓ 不影响正式版本 (0.1.0-alpha.1 等)
✓ 时间戳确保唯一性
✓ 可追溯到具体发布时间
✓ 使用 npm tag 隔离
```

### 2. 版本同步

```
SDK 和 CLI 使用相同的 canary 版本号

示例:
    open-agent-sdk@0.1.0-canary.202602271430
    @open-agent-sdk/cli@0.1.0-canary.202602271430

原因:
✓ 简化管理
✓ 确保版本对应清晰
```

### 3. 依赖精确版本

```
CLI 的 package.json:
    {
      "dependencies": {
        "open-agent-sdk": "0.1.0-canary.202602271430"  ← 无 ^，精确版本
      }
    }

原因:
✓ 确保 CLI 使用特定版本的 SDK
✓ 避免版本漂移
```

### 4. npm 标签隔离

```
发布时使用 --tag canary

结果:
    npm install @open-agent-sdk/cli           # latest (正式版本)
    npm install @open-agent-sdk/cli@canary    # 最新 canary
    npm install @open-agent-sdk/cli@0.1.0-canary.202602271430  # 特定版本

优点:
✓ canary 不会成为 latest
✓ 不影响普通用户
✓ 测试用户明确选择
```

## 🚀 使用流程

### 完整示例

```bash
# 1. 修改代码 (SDK 或 CLI)
vim packages/core/src/agent/react.ts

# 2. 本地测试
bun test

# 3. 一键发布
bun run publish:benchmark

# 输出:
# ╔════════════════════════════════════════════════════════╗
# ║     Benchmark Publishing Tool - SDK + CLI             ║
# ╚════════════════════════════════════════════════════════╝
#
# 📦 Generated canary version: 0.1.0-canary.202602271430
# 📝 Updating SDK version...
# 🧪 Running SDK tests...
# ▶ Building SDK
# 📤 Publishing SDK to npm...
# ⏳ Waiting for npm...
# 📝 Updating CLI version...
# 📤 Publishing CLI to npm...
#
# ╔════════════════════════════════════════════════════════╗
# ║                 ✅ PUBLISH COMPLETE!                   ║
# ╚════════════════════════════════════════════════════════╝
#
# 🚀 Install on Daytona:
#    npm install -g @open-agent-sdk/cli@0.1.0-canary.202602271430

# 4. 在 Daytona 上安装
npm install -g @open-agent-sdk/cli@0.1.0-canary.202602271430

# 5. 运行 benchmark
oas benchmark

# 6. (可选) 还原 package.json
git checkout packages/*/package.json
```

## ✅ 优势

### 1. 简单易用
- 一个命令完成所有步骤
- 无需记忆复杂流程
- 清晰的进度提示

### 2. 快速迭代
- 无需等待 CI/CD
- 本地直接发布
- 适合频繁测试

### 3. 安全可靠
- 自动运行测试
- 等待 npm 索引
- 精确版本依赖

### 4. 不影响正式版本
- 使用 canary 标签
- 独立的版本号
- 可以随时清理

## ⚠️ 注意事项

### 1. npm 认证
需要提前登录 npm：
```bash
npm login
npm whoami
```

### 2. package.json 修改
发布后会修改 `package.json`，建议还原：
```bash
git checkout packages/*/package.json
```

### 3. 发布权限
需要有两个包的发布权限：
- `open-agent-sdk`
- `@open-agent-sdk/cli`

### 4. 版本累积
canary 版本会累积，定期清理：
```bash
npm deprecate open-agent-sdk@0.1.0-canary.202602271430 "Old canary"
```

## 📊 与原始需求对比

### 你的需求 ✅
- [x] 修改 SDK 后，能快速发布
- [x] CLI 自动使用最新的 SDK
- [x] 适合 Daytona benchmark 场景
- [x] 可以频繁发布测试

### 未实现 (不在当前需求范围)
- [ ] GitHub Actions 自动化
- [ ] 正式版本发布流程
- [ ] 版本号独立管理
- [ ] 大而全的发布系统

## 🔄 后续扩展 (可选)

如果将来需要，可以扩展：

### 1. GitHub Actions 自动化
```yaml
# .github/workflows/publish-canary.yml
# 推送到特定分支自动发布 canary
```

### 2. 正式版本发布
```bash
bun run publish:release
# 发布 alpha/beta/rc/stable 版本
```

### 3. 版本独立管理
```bash
bun run publish:sdk 0.1.2
bun run publish:cli 0.2.1
# SDK 和 CLI 使用不同版本号
```

### 4. 自动清理旧版本
```bash
bun run clean:canary
# 自动 deprecate 7 天前的 canary 版本
```

## 📚 文档结构

```
docs/
├── BENCHMARK-PUBLISHING.md      # 最详细，包含所有场景和故障排除
├── PUBLISH-FLOW.md              # 流程图和场景分析
├── QUICK-PUBLISH.md             # 一页纸快速参考
└── publishing/
    └── README.md                # 文档导航
```

推荐阅读顺序：
1. QUICK-PUBLISH.md - 快速上手
2. BENCHMARK-PUBLISHING.md - 详细了解
3. PUBLISH-FLOW.md - 深入理解

## 🎉 总结

这个方案：
- ✅ **专注**: 只解决 Benchmark 发布问题
- ✅ **简单**: 一个命令完成所有步骤
- ✅ **可靠**: 自动测试、等待索引、精确依赖
- ✅ **灵活**: 可以随时扩展其他发布方式

完美匹配你的 Benchmark 测试场景！🚀
